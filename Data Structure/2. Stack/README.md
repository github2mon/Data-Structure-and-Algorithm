```cpp
ADT Stack
{
	init(&S);
	destory(&S);
	isEmpty(S);
	isFull(S);
	clear(&S);
	length(S);
	top(S);
	push(&S, e);
	pop(&S);
} ADT Stack
```
`always used in Infix expressions and postfix expressions`
