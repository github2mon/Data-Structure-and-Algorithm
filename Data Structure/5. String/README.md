```cpp
ADT String
{
    assign(&s1, s2);
    isEmpty(s);
    length(s);
    comp(s1, s2);
    concat(&s1, s2);
    subString(s, i, len);
    getIndex(s1, s2); //get the index of s2 in s1
    replace(&s, s1, s2); //replace the sub-string s1 as s2
    copy(s1, s2);
    insert(s1, i, s2);
    delete(s, i, k);
} ADT String
```
